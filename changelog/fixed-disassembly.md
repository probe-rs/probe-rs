probe-rs: Disassembly now takes the endianness of source files into account and no longer relies on host endianness.
probe-rs: tests: Faking of probes with access to program binaries is now possible.
probe-rs-tools: Fixed disassembly of ARM Thumb v2 code in the DAP server (proper instruction alignment and offset).
probe-rs-tools: tests: Added integration test for disassembly.
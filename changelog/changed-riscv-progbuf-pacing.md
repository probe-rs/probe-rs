RISC-V memory access through the program buffer now waits for every program that the debug module starts, so memory that is slower than the core no longer reads the value that a register held before the program wrote it. A debug module does not always report `cmderr` for an access that comes in too early. `MemoryAccessMethod::WaitingProgramBuffer` is gone, and a debug sequence no longer selects the wait per memory region.

The DAP server no longer writes the base64 file payloads of the `launch`, `attach`, and `restart` arguments to the log and to the debug console of the client. A program binary made each of these messages tens of megabytes, which blocked the server and the client for a long time, in particular after a restart, because the client repeats the launch arguments in each restart request.

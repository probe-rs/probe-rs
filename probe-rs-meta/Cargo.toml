[package]
name = "probe-rs-meta"
version = "0.1.0"
edition = "2024"
description = "Embed probe-rs metadata into ELF binaries"
repository = "https://github.com/probe-rs/probe-rs"
license = "MIT OR Apache-2.0"
categories = [
    "embedded",
    "no-std",
]

[dependencies]

[workspace]
members = [
    "cli",
    "debugger",
    "gdb-server",
    "probe-rs",
    "probe-rs-cli-util",
    "probe-rs-target",
    "rtt",
    "rtthost",
    "smoke-tester",
    "target-gen",
    "xtask",
]

[profile.release]
debug = true
split-debuginfo = "unpacked"

[profile.dev]
split-debuginfo = "unpacked"

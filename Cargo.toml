[workspace]
members = [
    "probe-rs",
    "probe-rs-target",
    "probe-rs-cli-util",
    "cli",
    "debugger",
    "gdb-server",
    "xtask",
    "smoke-tester",
]

[profile.release]
debug = true
split-debuginfo = "unpacked"

[profile.dev]
split-debuginfo = "unpacked"
